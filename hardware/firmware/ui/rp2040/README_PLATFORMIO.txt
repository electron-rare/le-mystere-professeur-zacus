Ce dossier n'utilise plus de platformio.ini local.
Utilisez le fichier platformio.ini à la racine du dossier firmware pour builder ce firmware :

  cd ../../
  pio run -e ui_rp2040_ili9488

Voir la documentation racine pour plus d'options.