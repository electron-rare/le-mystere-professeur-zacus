# Test wrappers (firmware workspace)

Canonical implementation lives in repo root:

- `tools/test/`

This folder only provides thin wrappers so you can run commands from
`hardware/firmware` context without path juggling.
