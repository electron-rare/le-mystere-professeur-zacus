fastapi>=0.115
httpx>=0.27
numpy>=1.26
python-multipart>=0.0.9
soundfile>=0.12
uvicorn[standard]>=0.30
# Pydantic models on /tts and /voice/intent (validation + 422)
pydantic>=2.0
# Per-IP rate limiting on /tts (slowapi wraps FastAPI cleanly)
slowapi>=0.1.9
# F5-TTS in-process backend (Apple Silicon MLX)
f5-tts-mlx>=0.2.6
mlx>=0.31
# Kyutai STT streaming client (kyutai_stt.py) — talks msgpack to
# moshi-server on :8304. scipy only used for the 16 kHz → 24 kHz
# polyphase resample.
msgpack>=1.0
websockets>=12
scipy>=1.10
# STT post-correction alias table loader (game/scenarios/stt_aliases.yaml).
pyyaml>=6
