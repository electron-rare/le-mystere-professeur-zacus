# Artifact QA/Test — validator bootstrap

- Validator dependencies pinned in `tools/requirements/validators.txt`.
- Bootstrap script added: `tools/setup/install_validators.sh`.
- Validation gates executed after install.
