Legacy payload mirrors are stored here on purpose.

- Files in this folder are kept for reference/migration only.
- They are outside `data/` so they are not embedded into LittleFS (`uploadfs`).
- Runtime canonical screen payloads stay in `data/story/screens/`.
