Generated LVGL font sources (.c) are emitted here by:

`tools/fonts/scripts/generate_lvgl_fonts.sh`

The runtime currently uses built-in Montserrat fallbacks unless `UI_FONT_EXTERNAL_SET=1`
and the generated files are present/compiled.
